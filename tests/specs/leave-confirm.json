[
  {
    "name": "Leave trip requires an active trip",
    "steps": [
      {
        "send": { "callback": "leave:confirm" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "You don't have an active trip. Create one first!" } }
        ]
      }
    ]
  }
]
